[routes]
ngramCorpora.route = "ngram/corpora/:action"
ngramCorpora.defaults.module = ngram
ngramCorpora.defaults.controller = index
ngramCorpora.defaults.action = browse

ngramId.route = "ngram/corpora/:action/:id"
ngramId.defaults.module = ngram
ngramId.defaults.controller = index
ngramId.reqs.id = "\d+"
